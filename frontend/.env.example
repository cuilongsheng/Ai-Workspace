# Leave unset in local development to use http(s)://<current-host>:3000.
# Set an absolute API origin for split-domain deployments, for example:
# VITE_API_BASE_URL=https://api.demo.example.com
VITE_API_BASE_URL=http://localhost:3000
